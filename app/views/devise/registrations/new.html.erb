<h2>Sign up</h2>
<p> <strong>*NOTE*</strong> All of the values below are default values for quick testing purposes. FYI - The default password value is <em>password</em></p>
<p> Please feel free to edit/customize any login information. </p>
<br />

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, autofocus: true, value: 'default_name' %>
  </div>

  <div class="field">
    <%= f.label :accountname %><br />
    <%= f.text_field :accountname, value: 'default_account' %>
  </div>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, value: 'email@email.com' %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off", value: 'password' %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off", value: 'password' %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
